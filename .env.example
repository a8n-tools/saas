# Docker Compose Configuration
# Copy this file to .env and update the values
#
# This file contains ONLY variables needed by Docker Compose itself
# (shared across services or compose-level concerns).
#
# Service-specific configuration lives in:
#   api/.env.example      — API server settings
#   frontend/.env.example — Frontend settings

# =============================================================================
# Database (shared by postgres + api services)
# =============================================================================
POSTGRES_PASSWORD=devpassword

# =============================================================================
# Production — Docker image versions
# =============================================================================
# API_VERSION=latest
# FRONTEND_VERSION=latest
